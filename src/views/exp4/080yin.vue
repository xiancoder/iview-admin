<template>
    <div class="blogCss">
        <div class="blog">
            <div class="blogTitle">名词缩写发音含义</div>
            <div class="blogContent" v-highlight>
                <Row>
                    <Col span="8">
                        <pre><code>W3C</code> (技术标准机构)</pre>
                        <pre><code>BFC</code> (样式布局 块级格式化上下文)</pre>
                        <pre><code>FOUC</code> (加载网页时出现的短暂的CSS样式失效)</pre>
                        <pre><code>Hack</code> (CSS兼容解决方案)</pre>
                        <pre><code>Sprite</code> (网页图片合并处理方式)</pre>
                        <pre><code>SEO优化</code> (搜索引擎排名识别依据) <br />搜索引擎不能收录 ajax 之后再动态 js 渲染的页面</pre>
                        <pre><code>SSR</code> (服务端渲染 server side render)</pre>
                        <pre><code>CSR</code> (客户端渲染 custom side render)</pre>
                        <pre><code>HTML</code> (H,T,M,L)</pre>
                        <pre><code>XML</code> (叉妹欧)</pre>
                        <pre><code>SQL</code> (色Q 数据库)</pre>
                        <pre><code>linux</code> (林尼克斯)</pre>
                        <pre><code>ubuntu</code> (乌班.图 | linux的一个版本)</pre>
                        <pre><code>glup</code> (狗泼 | 压缩代码的系统)</pre>
                        <pre><code>webpack</code> (web 批克 | 打包系统)</pre>
                        <pre><code>nodejs</code> (新一代服务器环境JS崛起)</pre>
                        <pre><code>Cordova</code> (科尔.多.瓦 | nodejs搞APP)</pre>
                    </Col>
                    <Col span="8">
                        <pre><code>mvc</code> <code>mvp</code> <code>mvvm</code> (视图逻辑分离)</pre>
                        <pre><code>F/B</code> (前后端分离)</pre>
                        <pre><code>cli</code> (脚手架)</pre>
                        <pre><code>NPM</code> (包管理工具)</pre>
                        <pre><code>WebPack</code> (模块打包)</pre>
                        <pre><code>Angular.js</code> (安.谷拉 | 后台框架)</pre>
                        <pre><code>Ionic</code> (爱.欧.尼克 | 移动端框架)</pre>
                        <pre><code>Vue.js</code> (威.欧 或者 V,U,E | 后台框架)</pre>
                        <pre><code>karma</code> (卡.嘛 | 跨浏览器单元测试框架)</pre>
                        <pre><code>Mocha</code> (摩.卡 | 摩卡咖啡 | 单元测试)</pre>
                        <pre><code>Wepy</code> (维.配 | 小程序组件化框架)</pre>
                        <pre><code>React.js</code> (瑞.爱可特 | 牛逼库)</pre>
                        <pre><code>Redux</code> (瑞.达科斯 | 可预测状态容器)</pre>
                        <pre><code>Mixin</code> (迷.思应~ missing | 反向注入的一种继承)</pre>
                        <pre><code>Router</code> (如奥.特耳 | 路由)</pre>
                        <pre><code>Axios</code> (艾克.瑟斯 | ajax优化封装)</pre>
                    </Col>
                    <Col span="8">
                        <pre><code>GPU</code> (图形处理器，显卡的心脏)</pre>
                        <pre><code>UA</code> (UA是User Agent的缩写)</pre>
                        <pre><code>SPA</code> (桑拿 或者指单页Web应用)</pre>
                        <pre><code>CDN</code> (经策略性部署的整体系统)</pre>
                        <pre><code>ERP</code> (企业资源计划)</pre>
                        <pre><code>OA</code> (办公自动化)</pre>
                        <pre><code>CMS</code> (内容管理系统)</pre>
                        <pre><code>CRM</code> (客户关系管理)</pre>
                        <pre><code>GIS</code> (地理信息系统)</pre>
                        <pre><code>IaaS</code> (基础设施即服务)</pre>
                        <pre><code>PaaS</code> (平台即服务，也叫做中间件)</pre>
                        <pre><code>SaaS</code> (软件即服务)</pre>
                        <pre><code>Shim</code> (优雅降级) <br />指的是在一个旧的环境中模拟出一个新 API </pre>
                    </Col>
                </Row>
            </div>
            <div class="blogFooter">
                <Tag color="green">green</Tag>
                <Tag color="cyan">cyan</Tag>
                <Tag color="blue">blue</Tag>
            </div>
        </div>
    </div>
</template>
<script>
export default {
    data () {
        return {}
    },
    methods: {
    },
    mounted () {
    }
}
</script>
